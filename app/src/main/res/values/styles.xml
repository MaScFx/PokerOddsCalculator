<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="MyCardView" parent="Theme.MaterialComponents.Light">
        <item name="android:layout_width">57dp</item>
        <item name="android:layout_height">89dp</item>
        <item name="cardCornerRadius">4dp</item>
        <item name="cardElevation">4dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="background">@drawable/card_background</item>
    </style>

    <style name= "FlopTurnRiverBackground" parent="Theme.MaterialComponents.Light">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:padding">4dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:background">@drawable/card_background</item>
    </style>

</resources>